(+module socket/0.0.1
  (note
    (export
      <socket>
      tcp-socket-listen
      socket-accept
      tcp-socket-connect
      socket-send
      socket-recv
      socket-close))

  current-reading-dir "socket.so" string-append
  lib-open :lib!
  :lib "expose" lib-call)
