(= nat (load "to-test-load.jo"))

(= zero nat.zero)
(= one nat.one)
(= two nat.two)
(= three nat.three)
(= four nat.four)
(= nat-add nat.nat-add)
(= nat-mul nat.nat-mul)

(assert (eq (nat-add two two) four))
(assert (eq (nat-mul two two) four))
