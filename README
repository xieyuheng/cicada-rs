//// cicada language society

* cicada-language

  - simply typed language with
    - subtype relation
    - type-class
    - quotient-type

* cicada-prover

  - dependently typed language
    - it can be viewed as a typed logic programming language
    - it can be used as a prover

* cicada-continua

  - a language with higher-inductive-type
    - it can be viewed as higher dimensional algebraic structure
      - homotopy groups are its sub-algebras
      - its abelianization are homology groups
    - it captures the concept of continuum
      by viewing boundary as type
    - it provide a language
      to describe constructions in algebraic topology
