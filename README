(jojo -- 珠珠

  (* jojo is a dynamic language, written in C,
     implemented as a dynamic threaded code interpreter.)

  (* features :

     (* object system,
        with single inheritance
        and multiple dispatching.)

     (* a simply module system.)

     (* a low level syntax extending mechanism,
        in which syntactic keywords are threaded code compilers.))

  (* source : https://github.com/xieyuheng/jojo))
