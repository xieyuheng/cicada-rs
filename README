jojo is a dynamicly typed language,
  - the syntax of which is optimized for function composition,
    instead of function application.
  - which uses a contract system to make it easier
    to reason about mistakes in code.
  - which uses a actor model to write distributed applications.
  - which is implemented in c, with the help of org-mode.