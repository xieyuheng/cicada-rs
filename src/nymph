#!/usr/bin/env node

const fs = require("fs");
const path = require("path");

const cicada = require("./nymph.js");

function main ()
{
    let arg_vect = process.argv.slice(2);
    for (let arg of arg_vect) {
        let code =
            fs.readFileSync(arg)
            .toString();
        let env = cicada.eval_code (code);
        env.data_stack_print_vertically ();
    }
}

main ();
