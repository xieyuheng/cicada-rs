(= (char-to-num char) ...)
(= (num-to-char num) ...)

(= (caesar-cipher text shift)
   (= (char-cipher char)
      (> char char-to-num (add shift) num-to-char))
   (str-map text char-cipher))

(= (caesar-decipher text shift)
   (= (char-decipher char)
      (> char char-to-num (sub shift) num-to-char))
   (str-map text char-decipher))

(= (char-frequency text)
   ...)

(= english-char-frequency-fingerprint
   ...)

(= (caesar-codebreaker text)
   ...)
